{
  "scripts": {
    "dev": "vuepress dev webApp",
    "build": "vuepress build webApp",
    "deploy": "yarn build && bash deploy.sh"
  },
  "dependencies": {
    "vuepress": "^1.5.4",
    "vuepress-theme-reco": "^1.5.5"
  }
}
